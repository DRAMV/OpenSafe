# OpenSafe
**OpenSafe** is a simple, robust, command-line utility designed to securely manage your passwords locally. It is built entirely in **C++11** and operates via an interactive terminal menu, featuring strong password generation and persistent storage to a binary file (`passwords.bin`).
